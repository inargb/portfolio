// Global eggs (present on every page). Page-specific eggs (shelf, hero
// word) are imported by their own components.
import './console';
import './spec-mode';
import './doodle';
import './shutter';
import './shortcuts';
